# ~/.tmuxinator/itux.yml

name: cat
root: ~/Projects/

startup_pane: 1

windows:
  - cat: 
      layout: even-horizontal
      panes:
        - pushd content-aggregation-transformation; ./start-backend.sh
        - pushd content-aggregation-transformation; ./start-dashboard-frontend.sh
  - shell:
      layout: even-horizontal
      panes:
        - zsh
        - zsh
  - test:
      layout: even-horizontal
      panes:
        - ssh h2921@10.64.100.109
